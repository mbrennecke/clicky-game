import React from "react";
import "./Instructions.css";

const Instructions = () => <h2 className="ins">Click on an image to earn points, but don't click on any more than once!</h2>

export default Instructions;